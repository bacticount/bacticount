apply plugin: 'com.android.application'

android {
    signingConfigs {
        garyfox {
            keyAlias 'garyfox'
            keyPassword 'thelabwiththeviewbythesea2018!'
        }
    }
    compileSdkVersion 26
    buildToolsVersion "21.1.2"
    defaultConfig {
        applicationId "com.garynfox.pathogenanalyzer"
        minSdkVersion 14
        targetSdkVersion 26
        testInstrumentationRunner "android.support.test.runner.AndroidJUnitRunner"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
    }
    productFlavors {
    }
    packagingOptions {
        exclude 'META-INF/NOTICE.txt'
        exclude 'META-INF/LICENSE.txt'
    }

    ///*added by android's own recommendation?
    lintOptions {
        checkReleaseBuilds false
        // Or, if you prefer, you can continue to check for errors in release builds,
        // but continue the build even when errors are found:
        abortOnError false
    }
    //*/


    sourceSets {main { jni.srcDirs = ['src/main/jni', 'src/main/jniLibs/'] } }
    externalNativeBuild {
        ndkBuild {
            path '../../../Downloads/OpenCV-android-sdk/sdk/native/jni/OpenCV.mk'
        }
    }
}

dependencies {
    compile project(':aFileDialog')
    compile project(':openCVLibrary310')
    compile project(':opencsv')
    compile 'com.android.support:appcompat-v7:26.1.0'
    compile files('libs/commons-math3-3.5-javadoc.jar')
    compile files('libs/commons-math3-3.5.jar')
}
